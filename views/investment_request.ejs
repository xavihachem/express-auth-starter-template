<h3 class="mb-5">Investment Access</h3>

<div class="card mb-4">
    <div class="card-header bg-primary text-white">
        <h4><i class="fas fa-lock me-2"></i> Investment Access Required</h4>
    </div>
    <div class="card-body">
        <div class="text-center mb-4">
            <div class="mb-4">
                <i class="fas fa-wallet fa-4x text-muted"></i>
            </div>
            <h5>You need admin approval to access the investment platform</h5>
            <p class="text-muted">Our team will create a custom wallet for your deposits</p>
        </div>

        <div class="alert alert-info">
            <h6><i class="fas fa-info-circle me-2"></i> How it works:</h6>
            <ol class="mb-0">
                <li>Request investment access by clicking the button below</li>
                <li>Our admin team will review your request</li>
                <li>Once approved, you'll receive a custom deposit wallet</li>
                <li>You'll then have full access to the investment platform</li>
            </ol>
        </div>

        <% if (accessStatus === 'rejected') { %>
            <div class="text-center mt-4">
                <form action="/request-investment-access" method="post">
                    <button type="submit" class="btn btn-primary btn-lg">
                        <i class="fas fa-unlock-alt me-2"></i> Request Investment Access
                    </button>
                </form>
            </div>
        <% } else if (accessStatus === 'pending') { %>
            <div class="alert alert-warning">
                <div class="d-flex align-items-center">
                    <div class="me-3">
                        <i class="fas fa-hourglass-half fa-2x text-warning"></i>
                    </div>
                    <div>
                        <h5 class="mb-1">Your request is pending approval</h5>
                        <p class="mb-0">Our admin team is reviewing your request. You'll receive access once approved.</p>
                    </div>
                </div>
            </div>
        <% } %>
    </div>
</div>

<div class="text-center mt-4">
    <a href="/" class="btn btn-outline-primary">
        <i class="fas fa-arrow-left me-2"></i> Back to Dashboard
    </a>
</div>
