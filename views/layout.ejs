<!-- eslint-disable -->
<!-- stylelint-disable -->
<!-- An HTML file that serves as the template for the Express authentication starter kit -->
<!-- Contains links to various CSS and JavaScript libraries -->
<!-- Uses EJS templating language to insert dynamic content -->
<!-- Uses Bootstrap and Material Design Bootstrap for layout and styling -->
<!-- Displays success and error messages using the Noty library -->
<html>

<head>
    <title>
        Moonify
    </title>
    <!-- Links to CSS files for Font Awesome, Roboto font, and Material Design Bootstrap -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.2.0/mdb.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <!-- Links to CSS files for Noty -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.1/noty.min.css" integrity="sha512-leQJisLD6lgz70MzHnVgZCwklwq2xcNB1+lIZIaw17o5vNJ7nBVK7w/TqCE5cKg4nDjiLcN/g2Lu0uA4rc5fAw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    
    <!-- IntlTelInput for phone number input with country codes -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/intl-tel-input/17.0.19/css/intlTelInput.css" referrerpolicy="no-referrer" />

    <!-- Link to custom CSS file for layout -->
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/auth-cosmic.css">
    <link rel="stylesheet" href="/css/cosmic-text.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    
    <!-- Custom styles for modern UI with animations -->
    <style>
        /* Simple rotating logo styling */
        .navbar-logo-container {
            display: inline-block;
            position: relative;
            height: 50px;
            overflow: visible;
            z-index: 10;
        }
        
        .navbar-logo {
            height: 110px;
            width: auto;
            position: absolute;
            top: -30px; /* Push logo above the navbar top edge */
            left: -85px; /* Push logo further left */
            object-fit: contain;
            transform-origin: center;
            animation: logo-rotate 15s infinite linear; /* Slowed down to 15s */
            z-index: 2;
        }
        
        /* Animation for the 360° rotating logo with color change */
        @keyframes logo-rotate {
            0% {
                transform: rotate(0deg);
                filter: hue-rotate(0deg) brightness(1);
            }
            25% {
                transform: rotate(90deg);
                filter: hue-rotate(60deg) brightness(1.1);
            }
            50% {
                transform: rotate(180deg);
                filter: hue-rotate(180deg) brightness(1.2); /* Purple-blue shift */
            }
            75% {
                transform: rotate(270deg);
                filter: hue-rotate(240deg) brightness(1.1); /* Blue-indigo shift */
            }
            90% {
                transform: rotate(324deg);
                filter: hue-rotate(300deg) brightness(1.2); /* Purple-pink shift */
            }
            95% {
                transform: rotate(342deg);
                filter: hue-rotate(330deg) brightness(1.4) saturate(1.5); /* Intense red shift */
            }
            100% {
                transform: rotate(360deg);
                filter: hue-rotate(0deg) brightness(1);
            }
        }
        
        :root {
            --primary: #4361ee;
            --secondary: #3f37c9;
            --success: #4cc9f0;
            --info: #4895ef;
            --warning: #f72585;
            --danger: #ff4d6d;
            --light: #f8f9fa;
            --dark: #212529;
            --card-border-radius: 16px;
            --transition-speed: 0.3s;
        }
        
        body.auth {
            background-color: #f0f2f5;
        }
        body.platform {
            background-color: #ffffff;
        }
        body {
            font-family: 'Roboto', sans-serif;
        }
        
        /* Modern card styling */
        .card {
            border-radius: var(--card-border-radius);
            border: none;
            box-shadow: 0 4px 25px rgba(0, 0, 0, 0.05);
            transition: transform var(--transition-speed), box-shadow var(--transition-speed);
            overflow: hidden;
            margin-bottom: 1.5rem;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }
        
        .card-header {
            border-bottom: none;
            padding: 1.5rem;
        }
        
        .card-body {
            padding: 1.5rem;
        }
        
        /* Glassmorphism for platform cards to reveal cosmic background */
        body.platform .card {
            background: rgba(13, 25, 42, 0.5) !important;
            -webkit-backdrop-filter: blur(10px) !important;
            backdrop-filter: blur(10px) !important;
            border: 1px solid rgba(95, 145, 255, 0.3) !important;
            box-shadow: 0 0 20px rgba(95, 145, 255, 0.2) !important;
        }
        
        body.platform .card:hover {
            box-shadow: 0 0 30px rgba(95, 145, 255, 0.4) !important;
            border: 1px solid rgba(95, 145, 255, 0.5) !important;
        }
        
        /* Scrollable card content for platform pages */
        body.platform .card {
            display: flex;
            flex-direction: column;
            max-height: 75vh !important;
        }
        body.platform .card-body {
            flex: 1;
            overflow-y: auto;
            padding-right: 0.5rem;
        }
        /* Cosmic-themed scrollbar */
        body.platform .card-body::-webkit-scrollbar {
            width: 8px;
        }
        body.platform .card-body::-webkit-scrollbar-track {
            background: rgba(13,25,42,0.2);
        }
        body.platform .card-body::-webkit-scrollbar-thumb {
            background: rgba(95,145,255,0.5);
            border-radius: 4px;
        }
        body.platform .card-body::-webkit-scrollbar-thumb:hover {
            background: rgba(95,145,255,0.7);
        }
        body.platform .card-body {
            scrollbar-width: thin;
            scrollbar-color: rgba(95,145,255,0.5) rgba(13,25,42,0.2);
        }
        
        /* Dashboard welcome card sizing */
        body.platform .dashboard-welcome-card {
            width: 90vw !important;
            max-width: 1000px !important;
            min-height: 40vh !important;
            margin: 0.2rem auto 2rem !important;
        }
        /* Dashboard main (glass) card sizing */
        .dashboard-main-card {
            width: 100% !important;
            max-width: none !important;
            /* extend upward by 2rem into the gap without shifting the bottom */
            min-height: calc(80vh + 2rem) !important;
            margin: -2rem auto 2rem !important;
        }
        @media (min-width: 992px) {
            .dashboard-main-card {
                /* desktop: extend upward by 2rem */
                width: calc(100% + 16rem) !important;
                min-height: calc(80vh + 2rem) !important;
                margin: -2rem -8rem 2rem !important;
            }
        }
        
        /* Mobile override: nested cards fit container */
        @media (max-width: 991px) {
            body.platform .dashboard-welcome-card {
                width: 100% !important;
                max-width: none !important;
                margin: 0.5rem auto 2rem !important;
                min-height: calc(40vh + 100px) !important;
            }
        }
        
        /* Mobile override: shrink nested cards inside the main wrapper */
        @media (max-width: 991px) {
            .dashboard-main-card .container-fluid .card:not(.rankings-card) {
                width: 100% !important;
                max-width: none !important;
                margin: 0.5rem auto !important;
                min-height: auto !important;
            }
            /* Disable scroll on stats and completed tasks cards in mobile */
            .dashboard-main-card .card .card-body {
                overflow: hidden !important;
            }
            /* Rankings page mobile: enable scroll for Available Ranks */
            .rankings-page .col-md-7 .rankings-card {
                display: flex !important;
                flex-direction: column !important;
                height: 60vh !important;
            }
            .rankings-page .col-md-7 .rankings-card .card-body {
                flex: 1;
                overflow-y: auto !important;
                -webkit-overflow-scrolling: touch !important;
            }
        }
        
        /* Reduce nested cards inside the glass card */
        /* 
        .dashboard-main-card .container-fluid .card:not(.rankings-card) {
            width: 90% !important;
            max-width: 600px !important;
            margin: 1rem auto !important;
        }
        */
        
        /* Disable scroll inside welcome card */
        body.platform .dashboard-welcome-card .card-body {
            overflow: hidden !important;
        }
        
        @media (min-width: 992px) {
            /* Rankings page: desktop card heights */
            .rankings-page .rankings-card { min-height: 450px !important; }
            .rankings-page .p-3.border.rounded.bg-light { min-height: 300px !important; }
        }
        
        /* Modern navbar styling */
        .navbar {
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 0.8rem 1rem;
        }
        
        body.platform .navbar {
            background-color: #ffffff !important;
        }
        
        .navbar-brand {
            font-weight: 700;
            letter-spacing: 1px;
            color: #fff !important;
        }
        
        .nav-link {
            position: relative;
            margin: 0 0.3rem;
            padding: 0.5rem 0.8rem !important;
            border-radius: 8px;
            transition: all var(--transition-speed);
            color: #fff !important;
        }
        
        .nav-link:hover {
            background-color: rgba(255, 255, 255, 0.1);
        }
        
        .nav-link::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: 0;
            left: 50%;
            background-color: white;
            transition: all var(--transition-speed);
            transform: translateX(-50%);
        }
        
        .nav-link:hover::after {
            width: 80%;
        }
        
        /* User code styling */
        .user-code {
            font-family: 'Roboto Mono', monospace;
            font-weight: bold;
            letter-spacing: 3px;
            font-size: 1.8rem;
            color: var(--primary);
            background-color: rgba(67, 97, 238, 0.05);
            padding: 1rem 1.5rem;
            border-radius: 12px;
            border: 2px solid var(--primary);
            display: inline-block;
            transition: all var(--transition-speed);
            box-shadow: 0 4px 15px rgba(67, 97, 238, 0.1);
        }
        
        .user-code:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 25px rgba(67, 97, 238, 0.2);
        }
        
        /* Modern buttons */
        .btn {
            border-radius: 10px;
            padding: 0.6rem 1.2rem;
            font-weight: 500;
            transition: all var(--transition-speed);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            border: none;
        }
        
        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
        }
        
        .btn-primary {
            background-color: var(--primary);
        }
        
        .btn-success {
            background-color: var(--success);
        }
        
        .btn-warning {
            background-color: var(--warning);
            color: white;
        }
        
        .btn-outline-light {
            border: 2px solid rgba(255, 255, 255, 0.8);
        }
        
        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        body.play-animations .animate-fade-in {
            animation: fadeIn 0.5s ease-out forwards;
        }
        
        body.play-animations .delay-1 { animation-delay: 0.1s; }
        body.play-animations .delay-2 { animation-delay: 0.2s; }
        body.play-animations .delay-3 { animation-delay: 0.3s; }
        body.play-animations .delay-4 { animation-delay: 0.4s; }
        
        /* Profile badge */
        .profile-badge {
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 50px;
            padding: 0.5rem 1rem;
            transition: all var(--transition-speed);
        }
        
        .profile-badge:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }
        
        /* Stats counter */
        .stat-counter {
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 0.5rem;
        }
        
        .stat-label {
            font-size: 0.9rem;
            color: #6c757d;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        /* Ensure dropdowns always appear on top */
        .dropdown-menu {
            z-index: 9999 !important;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            position: fixed;
        }
        
        /* Mobile dropdown positioning fix */
        @media (max-width: 991px) {
            .notification-dropdown,
            .profile-dropdown {
                position: fixed !important;
                top: 60px !important;
                right: 10px !important;
                left: auto !important;
                max-width: calc(100vw - 20px) !important;
                width: 280px !important;
                transform: none !important;
            }
        }
        
        /* Notification dropdown styling */
        .notification-dropdown {
            background-color: rgba(33, 37, 41, 0.95) !important;
            border: 1px solid rgba(95, 145, 255, 0.3) !important;
            backdrop-filter: blur(10px) !important;
            box-shadow: 0 5px 25px rgba(0, 0, 0, 0.3) !important;
            padding: 0.5rem 0 !important;
            border-radius: 0.5rem !important;
        }
        
        .notification-dropdown .dropdown-header {
            color: white !important;
            font-weight: 600 !important;
            font-size: 0.9rem !important;
            padding: 0.5rem 1rem !important;
        }
        
        .notification-dropdown .dropdown-divider {
            border-color: rgba(255, 255, 255, 0.1) !important;
            margin: 0.25rem 0 !important;
        }
        
        .notification-dropdown .dropdown-item {
            color: rgba(255, 255, 255, 0.9) !important;
            transition: all 0.2s ease !important;
            padding: 0.7rem 1rem !important;
            overflow: hidden !important;
        }
        
        /* Fix for notification message overflow */
        .notification-dropdown .dropdown-item small.text-muted,
        .notification-dropdown .dropdown-item p.small {
            overflow: hidden !important;
            text-overflow: ellipsis !important;
            white-space: nowrap !important;
            max-width: 200px !important;
            display: block !important;
        }
        
        .notification-dropdown .dropdown-item:hover {
            background-color: rgba(95, 145, 255, 0.2) !important;
        }
        
        .notification-dropdown .dropdown-item.text-muted {
            color: rgba(200, 200, 200, 0.7) !important;
        }
        
        .notification-dropdown .dropdown-item small.text-muted {
            color: rgba(200, 200, 200, 0.6) !important;
        }
        
        .notification-dropdown .dropdown-item:last-child,
        .profile-dropdown .dropdown-item:last-child {
            background-color: rgba(13, 25, 42, 0.5) !important;
            margin-top: 0.25rem !important;
            font-weight: 500 !important;
        }
        
        /* Gold username styling */
        .username-gold {
            background: linear-gradient(to right, #FFD700, #FFA500) !important;
            -webkit-background-clip: text !important;
            background-clip: text !important;
            color: transparent !important;
            font-weight: 600 !important;
            margin-right: 4px !important;
            letter-spacing: 0.5px !important;
            text-shadow: 0 0 10px rgba(255, 215, 0, 0.3) !important;
        }
        
        .username-gold-large {
            background: linear-gradient(to right, #FFD700, #FFA500) !important;
            -webkit-background-clip: text !important;
            background-clip: text !important;
            color: transparent !important;
            font-weight: 700 !important;
            font-size: 1.2rem !important;
            letter-spacing: 0.5px !important;
            text-shadow: 0 0 10px rgba(255, 215, 0, 0.4) !important;
        }
        
        /* Profile avatar styling */
        .profile-avatar {
            border: 2px solid rgba(255, 215, 0, 0.7) !important;
            box-shadow: 0 0 10px rgba(255, 215, 0, 0.5) !important;
            transition: all 0.3s ease !important;
        }
        
        /* Desktop nav profile link */
        .navbar .dropdown .user-profile-link {
            padding: 5px 10px !important;
            border-radius: 50px !important;
            transition: all 0.3s ease !important;
        }
        
        .navbar .dropdown .user-profile-link:hover {
            background: rgba(255, 255, 255, 0.1) !important;
        }
        
        .user-profile-link:hover .profile-avatar {
            transform: scale(1.05) !important;
            box-shadow: 0 0 15px rgba(255, 215, 0, 0.8) !important;
        }
        
        /* Position the desktop dropdown properly */
        @media (min-width: 992px) {
            .dropdown-menu.premium-profile-dropdown {
                position: absolute !important;
                right: 0 !important;
                left: auto !important;
                transform: none !important;
                margin-top: 10px !important;
            }
        }
        
        /* Enhanced Profile dropdown styling */
        .premium-profile-dropdown {
            background: linear-gradient(135deg, rgba(20, 24, 33, 0.97), rgba(33, 37, 41, 0.97)) !important;
            border: 1px solid rgba(255, 215, 0, 0.3) !important;
            backdrop-filter: blur(10px) !important;
            -webkit-backdrop-filter: blur(10px) !important;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4) !important;
            padding: 0 0 8px 0 !important;
            border-radius: 12px !important;
            animation: fadeIn 0.3s ease-out !important;
            position: absolute !important;
            right: 0 !important;
            left: auto !important;
            max-height: 90vh !important;
            overflow-y: auto !important;
            overflow-x: hidden !important;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .user-card {
            background: linear-gradient(to bottom, rgba(255, 215, 0, 0.15), rgba(255, 215, 0, 0.05)) !important;
            border-bottom: 1px solid rgba(255, 215, 0, 0.2) !important;
        }
        
        .avatar-large {
            width: 60px !important;
            height: 60px !important;
            border-radius: 50% !important;
            overflow: hidden !important;
            border: 2px solid rgba(255, 215, 0, 0.7) !important;
            box-shadow: 0 0 15px rgba(255, 215, 0, 0.5) !important;
        }
        
        .menu-item {
            transition: all 0.2s ease !important;
            margin: 0 8px !important;
            border-radius: 8px !important;
            width: auto !important;
            white-space: nowrap !important;
        }
        
        .menu-item:hover {
            background-color: rgba(255, 255, 255, 0.1) !important;
            transform: translateX(3px) !important;
        }
        
        .sign-out-button {
            background-color: rgba(220, 53, 69, 0.1) !important;
            margin-top: 5px !important;
            margin-bottom: 5px !important;
        }
        
        .sign-out-button:hover {
            background-color: rgba(220, 53, 69, 0.2) !important;
        }
        
        .menu-icon {
            width: 32px !important;
            height: 32px !important;
            display: flex !important;
            align-items: center !important;
            justify-content: center !important;
            font-size: 0.9rem !important;
            transition: all 0.3s ease !important;
        }
        
        .menu-item:hover .menu-icon {
            transform: scale(1.15) !important;
        }
        
        .icon-primary {
            background: linear-gradient(135deg, #007bff, #0056b3) !important;
            color: white !important;
        }
        
        .icon-success {
            background: linear-gradient(135deg, #28a745, #1e7e34) !important;
            color: white !important;
        }
        
        .icon-info {
            background: linear-gradient(135deg, #17a2b8, #138496) !important;
            color: white !important;
        }
        
        .icon-danger {
            background: linear-gradient(135deg, #dc3545, #bd2130) !important;
            color: white !important;
        }
        
        .profile-dropdown .dropdown-header {
            background-color: rgba(0, 123, 255, 0.1) !important;
            color: rgba(255, 255, 255, 0.8) !important;
            font-size: 0.75rem !important;
            letter-spacing: 0.5px !important;
            padding: 0.5rem 1rem !important;
        }
        
        .profile-dropdown .dropdown-divider {
            border-color: rgba(255, 255, 255, 0.1) !important;
            margin: 0.25rem 0 !important;
        }
        
        .profile-dropdown .dropdown-item {
            color: rgba(255, 255, 255, 0.9) !important;
            transition: all 0.2s ease !important;
            padding: 0.7rem 1rem !important;
        }
        
        .profile-dropdown .dropdown-item:hover {
            background-color: rgba(95, 145, 255, 0.2) !important;
        }
        
        /* Icon colors for notification dropdown */
        .icon-bg-primary { background-color: #0d6efd !important; }
        .icon-bg-success { background-color: #198754 !important; }
        .icon-bg-danger { background-color: #dc3545 !important; }
        .icon-bg-warning { background-color: #ffc107 !important; }
        .icon-bg-info { background-color: #0dcaf0 !important; }
        .icon-bg-secondary { background-color: #6c757d !important; }
        
        /* Reset for navbar */
        nav {
            max-width: 100%;
            background: transparent !important;
            box-shadow: none !important;
            border: none !important;
            position: relative;
        }
        
        .navbar-container {
            display: flex;
            align-items: center;
        }
        
        .navbar-logo-container {
            position: relative;
            z-index: 1000;
            margin-left: 30px; /* Adjust space for the logo */
        }
        
        .nav-toggler-btn {
            background: transparent !important;
            border: none !important;
            width: 30px !important;
            height: 30px !important;
            position: relative !important;
            padding: 0.4rem !important; /* Reduced padding */
            display: none !important; /* Hide by default */
            flex-direction: column !important;
            justify-content: space-around !important;
            cursor: pointer !important;
            transition: all 0.3s !important;
        }
        
        /* Only show hamburger on mobile */
        @media (max-width: 991.98px) {
            .nav-toggler-btn {
                display: flex !important;
            }
        }
        
        .toggler-icon {
            width: 100% !important;
            height: 2px !important;
            background-color: white !important;
            border-radius: 2px !important;
            transition: all 0.3s !important;
            transform-origin: 1px !important;
        }
        
        .navbar-toggler[aria-expanded="true"] .toggler-icon:first-child {
            transform: rotate(45deg) !important;
        }
        
        .navbar-toggler[aria-expanded="true"] .toggler-icon:nth-child(2) {
            opacity: 0 !important;
            transform: translateX(-20px) !important;
        }
        
        .navbar-toggler[aria-expanded="true"] .toggler-icon:last-child {
            transform: rotate(-45deg) !important;
        }
        
        /* Navigation Menu Items */
        .nav-menu {
            gap: 8px !important;
        }
        
        .nav-btn {
            padding: 0.6rem 1rem !important;
            border-radius: 8px !important;
            transition: all 0.3s ease !important;
            position: relative !important;
            display: flex !important;
            align-items: center !important;
            overflow: hidden !important;
        }
        
        .nav-btn::before {
            content: '' !important;
            position: absolute !important;
            bottom: 0 !important;
            left: 0 !important;
            width: 100% !important;
            height: 100% !important;
            background: linear-gradient(to right, rgba(95, 145, 255, 0.1), transparent) !important;
            transform: translateX(-100%) !important;
            transition: transform 0.3s ease-out !important;
            z-index: -1 !important;
        }
        
        .nav-btn:hover {
            color: white !important;
            transform: translateY(-2px) !important;
        }
        
        .nav-btn:hover::before {
            transform: translateX(0) !important;
        }
        
        .nav-btn.active {
            background: rgba(95, 145, 255, 0.2) !important;
            box-shadow: 0 0 10px rgba(95, 145, 255, 0.3) !important;
            color: white !important;
        }
        
        .nav-icon {
            margin-right: 10px !important;
            font-size: 1rem !important;
            transition: all 0.3s ease !important;
        }
        
        .nav-btn:hover .nav-icon {
            transform: scale(1.2) !important;
        }
        
        /* Mobile optimizations */
        @media (max-width: 991.98px) {
            .nav-menu {
                padding: 1rem 0.5rem !important;
                max-height: 80vh !important;
                overflow-y: auto !important;
            }
            
            .nav-btn {
                padding: 0.8rem 1rem !important;
                margin-bottom: 5px !important;
                border-radius: 8px !important;
            }
            
            .nav-btn:active {
                background: rgba(95, 145, 255, 0.2) !important;
            }
            
            .navbar-collapse {
                background: rgba(13, 25, 42, 0.95) !important;
                border-radius: 12px !important;
                margin-top: 10px !important;
                padding: 0.5rem !important;
                border: 1px solid rgba(95, 145, 255, 0.2) !important;
                animation: slideDown 0.3s ease forwards !important;
                box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2) !important;
            }
            
            @keyframes slideDown {
                from { opacity: 0; transform: translateY(-10px); }
                to { opacity: 1; transform: translateY(0); }
            }
        }
        
        body.platform .navbar {
            background-color: transparent !important;
        }
        body.platform section.vh-100 {
            background-color: transparent !important;
        }
        /* Glass effect matching auth `.cosmic-card` */
        body.platform .card {
            background: rgba(13, 25, 42, 0.5) !important;
            -webkit-backdrop-filter: blur(10px) !important;
            backdrop-filter: blur(10px) !important;
            border: 1px solid rgba(95, 145, 255, 0.3) !important;
            box-shadow: 0 0 20px rgba(95, 145, 255, 0.2) !important;
        }
        body.platform .card:hover {
            box-shadow: 0 0 30px rgba(95, 145, 255, 0.4) !important;
            border: 1px solid rgba(95, 145, 255, 0.5) !important;
        }
        
        /* Cosmic background & transparent navbar on platform pages */
        body.platform {
            background: radial-gradient(ellipse at bottom, #0D1E31, #111 90%) !important;
            animation: backgroundPulse 15s ease-in-out infinite !important;
            position: relative !important;
            overflow: hidden !important;
        }
        
        /* Enhanced Navbar Styling */
        .navbar-glass {
            background: rgba(13, 25, 42, 0.5) !important;
            backdrop-filter: blur(10px) !important;
            -webkit-backdrop-filter: blur(10px) !important;
            border-bottom: 1px solid rgba(95, 145, 255, 0.2) !important;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15) !important;
            padding: 0.8rem 1rem !important;
            transition: all 0.3s ease !important;
            position: sticky !important;
            top: 0 !important;
            z-index: 1000 !important;
        }
        
        .navbar-glass:hover {
            background: rgba(13, 25, 42, 0.7) !important;
            border-bottom: 1px solid rgba(95, 145, 255, 0.3) !important;
        }
        
        /* Logo animation */
        .pulse-glow {
            animation: logoPulse 3s ease-in-out infinite !important;
        }
        
        @keyframes logoPulse {
            0% { filter: drop-shadow(0 0 2px rgba(95, 145, 255, 0.4)); }
            50% { filter: drop-shadow(0 0 10px rgba(95, 145, 255, 0.8)); }
            100% { filter: drop-shadow(0 0 2px rgba(95, 145, 255, 0.4)); }
        }
    </style>

    <!-- Scripts for Bootstrap, Material Design Bootstrap and Noty -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.2.0/mdb.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.1/noty.min.js"></script>
    
    <!-- Client-side script to refresh notifications count -->
    <script>
        // Function to fetch notification count via AJAX
        function checkForNewNotifications() {
            fetch('/check-notifications', {
                method: 'GET',
                headers: {
                    'Accept': 'application/json',
                    'X-Requested-With': 'XMLHttpRequest',
                    // Include CSRF token for security
                    'CSRF-Token': document.querySelector('meta[name="csrf-token"]').getAttribute('content')
                },
                credentials: 'same-origin'
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    // Update notification badges in both mobile and desktop views
                    const badgeElements = document.querySelectorAll('.notification-badge');
                    
                    badgeElements.forEach(badge => {
                        // If there are unread notifications, show the count and make badge visible
                        if (data.unreadCount > 0) {
                            badge.textContent = data.unreadCount;
                            badge.style.display = '';
                        } else {
                            // Otherwise hide the badge
                            badge.style.display = 'none';
                        }
                    });
                }
            })
            .catch(error => console.error('Error checking notifications:', error));
        }
        
        // Check for new notifications every 30 seconds
        let notificationInterval;
        
        // Custom script for mobile navbar and dropdowns
        document.addEventListener('DOMContentLoaded', function() {
            // Start the notification check interval if user is logged in
            if (document.querySelector('.notification-badge')) {
                // Check immediately and then every 30 seconds
                checkForNewNotifications();
                notificationInterval = setInterval(checkForNewNotifications, 30000);
            }
            
            // Initialize Bootstrap tooltips
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
            var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                return new bootstrap.Tooltip(tooltipTriggerEl)
            })
            
            // Only initialize navbar components if the navbar exists (not on login/signup pages)
            var navbarElement = document.getElementById('navbarNav');
            if (navbarElement) {
                // Let Bootstrap handle the toggle natively
                // This ensures proper opening and closing of the mobile menu
                var bsCollapse = new bootstrap.Collapse(navbarElement, {
                    toggle: false
                });
                
                // Add click handler to all nav links to close menu when clicked on mobile
                var navLinks = document.querySelectorAll('.navbar-nav .nav-link');
                navLinks.forEach(function(link) {
                    link.addEventListener('click', function() {
                        if (window.innerWidth < 992) { // Only on mobile view
                            bsCollapse.hide();
                        }
                    });
                });
                
                // Fix for dropdown menus to appear on top of all content
                var dropdownToggles = document.querySelectorAll('[data-bs-toggle="dropdown"]');
                dropdownToggles.forEach(function(toggle) {
                    toggle.addEventListener('click', function() {
                        // Small delay to ensure the dropdown is created
                        setTimeout(function() {
                            var dropdown = document.querySelector('.dropdown-menu.show');
                            if (dropdown) {
                                // Move dropdown to the body to avoid stacking context issues
                                document.body.appendChild(dropdown);
                                
                                // Position the dropdown correctly
                                var toggleRect = toggle.getBoundingClientRect();
                                var isUserDropdown = toggle.id === 'userDropdown';
                                
                                dropdown.style.position = 'fixed';
                                dropdown.style.zIndex = '9999';
                                dropdown.style.top = (toggleRect.bottom + window.scrollY) + 'px';
                                
                                if (isUserDropdown) {
                                    // Position the user dropdown to the right of the toggle button
                                        // Position the user dropdown correctly based on screen size
                                    if (window.innerWidth < 992) {
                                        dropdown.style.left = 'auto';
                                        dropdown.style.right = '10px';
                                    } else {
                                        dropdown.style.left = toggleRect.left + 'px';
                                    }
                                    dropdown.style.maxHeight = '200px';
                                    dropdown.style.overflowY = 'auto';
                                    dropdown.style.width = '200px';
                                } else {
                                    // For notification dropdown, position it more to the right
                                        // For notification dropdown, position it more to the right on desktop, centered on mobile
                                    if (window.innerWidth < 992) {
                                        dropdown.style.left = 'auto';
                                        dropdown.style.right = '10px';
                                    } else {
                                        dropdown.style.left = (toggleRect.left - 50) + 'px';
                                    }
                                    dropdown.style.maxHeight = '400px';
                                    dropdown.style.overflowY = 'auto';
                                    dropdown.style.width = '280px';
                                }
                                
                                // Add event listener to close dropdown when clicking outside
                                var closeDropdownHandler = function(e) {
                                    if (!dropdown.contains(e.target) && !toggle.contains(e.target)) {
                                        dropdown.classList.remove('show');
                                        document.removeEventListener('click', closeDropdownHandler);
                                    }
                                };
                                
                                // Delay adding the event listener to prevent immediate closing
                                setTimeout(function() {
                                    document.addEventListener('click', closeDropdownHandler);
                                }, 100);
                            }
                        }, 10);
                    });
                });
            }
        });
    </script>

    <!-- Guard animations: only on first load -->
    <script>
    document.addEventListener('DOMContentLoaded', function() {
        if (!sessionStorage.getItem('firstLoadDone')) {
            document.body.classList.add('play-animations');
            sessionStorage.setItem('firstLoadDone', 'true');
        } else {
            document.body.classList.remove('play-animations');
        }
    });
    </script>

    <!-- Insert dynamic CSS -->
    <%- style %>
    <% if (locals.user) { %>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Handle Support Ticket link clicks
            const supportTicketLinks = document.querySelectorAll('#supportTicketLink');
            supportTicketLinks.forEach(link => {
                link.addEventListener('click', function(e) {
                    e.preventDefault();
                    // Scroll to the contact support section on the dashboard
                    if (window.location.pathname === '/dashboard') {
                        const contactSupportSection = document.querySelector('#contactSupportSection');
                        const contactSupportPanel = document.getElementById('contactSupportPanel');
                        const contactSupportBtn = document.getElementById('contactSupportBtn');
                        
                        if (contactSupportSection) {
                            // Log the initial position of elements for diagnosis
                            const dashboardCards = document.querySelectorAll('.card');
                            console.log('--- SUPPORT TICKET CLICK - BEFORE ACTION ---');
                            console.log('Window scroll position:', window.pageYOffset);
                            
                            // Instead of scrolling, directly show the contact support panel
                            if (contactSupportBtn && contactSupportPanel) {
                                // Directly show the panel without scrolling
                                contactSupportPanel.style.display = 'flex';
                                document.body.style.overflow = 'hidden';
                                
                                console.log('Directly showing contact support panel without scrolling');
                            }
                        }
                    } else {
                        // Redirect to dashboard without anchor to avoid panel issues
                        window.location.href = '/dashboard';
                        
                        // Store in session that we want to open the support panel
                        sessionStorage.setItem('openSupportPanel', 'true');
                    }
                });
            });
        });
    </script>
    <style>
        /* Cosmic background & transparent navbar on platform pages */
        body.platform {
            background: radial-gradient(ellipse at bottom, #0D1E31, #111 90%) !important;
            animation: backgroundPulse 15s ease-in-out infinite !important;
            position: relative !important;
            overflow: hidden !important;
        }
                /* Enhanced Navbar Styling */
        .navbar-glass {
            background: rgba(13, 25, 42, 0.5) !important;
            backdrop-filter: blur(10px) !important;
            -webkit-backdrop-filter: blur(10px) !important;
            border-bottom: 1px solid rgba(95, 145, 255, 0.2) !important;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15) !important;
            padding: 0.8rem 1rem !important;
            transition: all 0.3s ease !important;
            position: sticky !important;
            top: 0 !important;
            z-index: 1000 !important;
        }
        
        .navbar-glass:hover {
            background: rgba(13, 25, 42, 0.7) !important;
            border-bottom: 1px solid rgba(95, 145, 255, 0.3) !important;
        }
        
        /* Logo animation */
        .pulse-glow {
            animation: logoPulse 3s ease-in-out infinite !important;
        }
        
        @keyframes logoPulse {
            0% { filter: drop-shadow(0 0 2px rgba(95, 145, 255, 0.4)); }
            50% { filter: drop-shadow(0 0 10px rgba(95, 145, 255, 0.8)); }
            100% { filter: drop-shadow(0 0 2px rgba(95, 145, 255, 0.4)); }
        }
        
        /* Hamburger Menu Animation */
        .nav-toggler-btn {
            background: transparent !important;
            border: none !important;
            width: 30px !important;
            height: 30px !important;
            position: relative !important;
            padding: 0.4rem !important; /* Reduced padding */
            display: none !important; /* Hide by default */
            flex-direction: column !important;
            justify-content: space-around !important;
            cursor: pointer !important;
            transition: all 0.3s !important;
        }
        
        /* Only show hamburger on mobile */
        @media (max-width: 991.98px) {
            .nav-toggler-btn {
                display: flex !important;
            }
        }
        
        .toggler-icon {
            width: 100% !important;
            height: 2px !important;
            background-color: white !important;
            border-radius: 2px !important;
            transition: all 0.3s !important;
            transform-origin: 1px !important;
        }
        
        .navbar-toggler[aria-expanded="true"] .toggler-icon:first-child {
            transform: rotate(45deg) !important;
        }
        
        .navbar-toggler[aria-expanded="true"] .toggler-icon:nth-child(2) {
            opacity: 0 !important;
            transform: translateX(-20px) !important;
        }
        
        .navbar-toggler[aria-expanded="true"] .toggler-icon:last-child {
            transform: rotate(-45deg) !important;
        }
        
        /* Navigation Menu Items */
        .nav-menu {
            gap: 8px !important;
        }
        
        .nav-btn {
            padding: 0.6rem 1rem !important;
            border-radius: 8px !important;
            transition: all 0.3s ease !important;
            position: relative !important;
            display: flex !important;
            align-items: center !important;
            overflow: hidden !important;
        }
        
        .nav-btn::before {
            content: '' !important;
            position: absolute !important;
            bottom: 0 !important;
            left: 0 !important;
            width: 100% !important;
            height: 100% !important;
            background: linear-gradient(to right, rgba(95, 145, 255, 0.1), transparent) !important;
            transform: translateX(-100%) !important;
            transition: transform 0.3s ease-out !important;
            z-index: -1 !important;
        }
        
        .nav-btn:hover {
            color: white !important;
            transform: translateY(-2px) !important;
        }
        
        .nav-btn:hover::before {
            transform: translateX(0) !important;
        }
        
        .nav-btn.active {
            background: rgba(95, 145, 255, 0.2) !important;
            box-shadow: 0 0 10px rgba(95, 145, 255, 0.3) !important;
            color: white !important;
        }
        
        .nav-icon {
            margin-right: 10px !important;
            font-size: 1rem !important;
            transition: all 0.3s ease !important;
        }
        
        .nav-btn:hover .nav-icon {
            transform: scale(1.2) !important;
        }
        
        /* Mobile optimizations */
        @media (max-width: 991.98px) {
            .nav-menu {
                padding: 1rem 0.5rem !important;
                max-height: 80vh !important;
                overflow-y: auto !important;
            }
            
            .nav-btn {
                padding: 0.8rem 1rem !important;
                margin-bottom: 5px !important;
                border-radius: 8px !important;
            }
            
            .nav-btn:active {
                background: rgba(95, 145, 255, 0.2) !important;
            }
            
            .navbar-collapse {
                background: rgba(13, 25, 42, 0.95) !important;
                border-radius: 12px !important;
                margin-top: 10px !important;
                padding: 0.5rem !important;
                border: 1px solid rgba(95, 145, 255, 0.2) !important;
                animation: slideDown 0.3s ease forwards !important;
                box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2) !important;
            }
            
            @keyframes slideDown {
                from { opacity: 0; transform: translateY(-10px); }
                to { opacity: 1; transform: translateY(0); }
            }
        }

        body.platform .navbar {
            background-color: transparent !important;
        }
        body.platform section.vh-100 {
            background-color: transparent !important;
        }
        /* Glass effect matching auth `.cosmic-card` */
        body.platform .card {
            background: rgba(13, 25, 42, 0.5) !important;
            -webkit-backdrop-filter: blur(10px) !important;
            backdrop-filter: blur(10px) !important;
            border: 1px solid rgba(95, 145, 255, 0.3) !important;
            box-shadow: 0 0 20px rgba(95, 145, 255, 0.2) !important;
        }
        body.platform .card:hover {
            box-shadow: 0 0 30px rgba(95, 145, 255, 0.4) !important;
            border: 1px solid rgba(95, 145, 255, 0.5) !important;
        }
    </style>
    <% } %>
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link rel="icon" href="/images/moonBlue.png" type="image/png">
</head>

<body class="<%= locals.user ? 'platform' : 'auth' %>">
    <% if (locals.user) { %>
    <div class="stars-bg" id="stars-bg"></div>
    <% } %>
    <% if (locals.user) { %>
    <!-- Modern navbar with enhanced animations and glass effect -->
    <nav class="navbar navbar-expand-lg mb-4 navbar-glass">
        <div class="container">
            <a class="navbar-brand animate-fade-in" href="/">
                <div class="navbar-logo-container">
                    <img src="/css/images/Monifylogo.png" alt="Moonify Logo" class="navbar-logo">
                </div>
            </a>
            
            <!-- Mobile-visible user controls - Only visible on small screens -->
            <div class="d-flex align-items-center order-lg-2">
                <div class="d-flex align-items-center d-lg-none me-2">
                    <!-- Notification Dropdown (Mobile) -->
                    <div class="dropdown me-2">
                        <a class="nav-link position-relative" href="#" id="notificationDropdownMobile" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-bell text-white"></i>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger notification-badge" style="<%= locals.notifications && !locals.notifications.hasUnread ? 'display: none;' : '' %>">
                                <% if (locals.notifications && locals.notifications.items) { %>
                                    <%= locals.notifications.items.filter(n => !n.read).length %>
                                <% } else { %>3<% } %>
                                <span class="visually-hidden">unread notifications</span>
                            </span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end position-absolute notification-dropdown" aria-labelledby="notificationDropdownMobile" style="min-width: 280px; max-width: 280px; right: 0; left: auto; transform: translateX(-70%); z-index: 9999 !important;">
                            <li><h6 class="dropdown-header py-1"><i class="fas fa-bell me-2"></i>Notifications</h6></li>
                            <li><hr class="dropdown-divider my-1"></li>
                            <!-- Notification items -->
                            <% if (locals.notifications && locals.notifications.items && locals.notifications.items.length > 0) { %>
                                <% locals.notifications.items.slice(0, 3).forEach(function(notification) { %>
                                    <li>
                                        <a class="dropdown-item d-flex align-items-center py-1 <%= notification.read ? 'text-muted' : '' %>" href="#">
                                            <div class="rounded-circle icon-bg-<%= notification.bgColor %> p-1 me-2 text-white" style="width: 24px; height: 24px; display: flex; align-items: center; justify-content: center;">
                                                <i class="fas fa-<%= notification.icon %> fa-sm"></i>
                                            </div>
                                            <div style="max-width: calc(100% - 30px); overflow: hidden;">
                                                <p class="mb-0 small <%= notification.read ? '' : 'fw-bold' %>" title="<%= notification.title %>"><%= notification.title %></p>
                                                <small class="text-muted" style="font-size: 0.7rem;" title="<%= notification.message %>"><%= notification.message %></small>
                                            </div>
                                        </a>
                                    </li>
                                    <% if (locals.notifications.items.indexOf(notification) < Math.min(2, locals.notifications.items.length - 1)) { %>
                                        <li><hr class="dropdown-divider my-1"></li>
                                    <% } %>
                                <% }); %>
                            <% } else { %>
                                <li>
                                    <a class="dropdown-item d-flex align-items-center py-1 text-muted" href="#">
                                        <div class="rounded-circle icon-bg-secondary p-1 me-2 text-white" style="width: 24px; height: 24px; display: flex; align-items: center; justify-content: center;">
                                            <i class="fas fa-bell-slash fa-sm"></i>
                                        </div>
                                        <div style="max-width: calc(100% - 30px); overflow: hidden;">
                                            <p class="mb-0 small">No notifications</p>
                                            <small class="text-muted" style="font-size: 0.7rem;">You're all caught up!</small>
                                        </div>
                                    </a>
                                </li>
                            <% } %>
                            <li><hr class="dropdown-divider my-1"></li>
                            <li><a class="dropdown-item text-center py-1 small" href="/notifications"><i class="fas fa-external-link-alt me-1"></i>View all notifications</a></li>
                        </ul>
                    </div>
                    
                    <!-- User Dropdown (Mobile) -->
                    <div class="dropdown">
                        <a href="#" class="text-decoration-none dropdown-toggle user-profile-link" id="userDropdownMobile" data-bs-toggle="dropdown" data-bs-auto-close="outside" aria-expanded="false">
                            <div class="rounded-circle overflow-hidden profile-avatar" style="width: 28px; height: 28px;">
                                <% if (locals.user.avatar) { %>
                                <img src="<%= locals.user.avatar %>" alt="<%= locals.user.name %>" class="w-100 h-100" style="object-fit: cover;">
                                <% } else { %>
                                <div class="bg-primary w-100 h-100 d-flex align-items-center justify-content-center text-white">
                                    <i class="fas fa-user"></i>
                                </div>
                                <% } %>
                            </div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end position-absolute premium-profile-dropdown" aria-labelledby="userDropdownMobile" style="width: 240px; right: 0; left: auto; z-index: 9999 !important;">
                            <li class="user-card mb-2">
                                <div class="text-center p-3">
                                    <div class="avatar-large mx-auto mb-2">
                                        <% if (locals.user.avatar) { %>
                                        <img src="<%= locals.user.avatar %>" alt="<%= locals.user.name %>" class="w-100 h-100" style="object-fit: cover;">
                                        <% } else { %>
                                        <div class="bg-primary w-100 h-100 d-flex align-items-center justify-content-center text-white">
                                            <i class="fas fa-user"></i>
                                        </div>
                                        <% } %>
                                    </div>
                                    <h6 class="mb-0 username-gold-large"><%= locals.user.name %></h6>
                                    <small class="text-muted">Member since <%= new Date(locals.user.createdAt).toLocaleDateString() %></small>
                                </div>
                            </li>
                            <li>
                                <a class="dropdown-item menu-item py-2" href="/profile">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-primary me-2">
                                            <i class="fas fa-user-circle"></i>
                                        </div>
                                        <span>My Profile</span>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item menu-item py-2" href="/team">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-info me-2">
                                            <i class="fas fa-users"></i>
                                        </div>
                                        <span>My Team</span>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item menu-item py-2" href="/investments">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-success me-2">
                                            <i class="fas fa-chart-line"></i>
                                        </div>
                                        <span>Investments</span>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item menu-item py-2" href="#" id="supportTicketLink">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-info me-2">
                                            <i class="fas fa-headset"></i>
                                        </div>
                                        <span>Support Ticket</span>
                                    </div>
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item menu-item py-2 sign-out-button" href="/sign-out">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-danger me-2">
                                            <i class="fas fa-sign-out-alt"></i>
                                        </div>
                                        <span class="fw-bold">Sign Out</span>
                                    </div>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
                
                <button class="navbar-toggler nav-toggler-btn" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="toggler-icon"></span>
                    <span class="toggler-icon"></span>
                    <span class="toggler-icon"></span>
                </button>
            </div>
            <div class="collapse navbar-collapse order-lg-1" id="navbarNav">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 nav-menu">
                    <li class="nav-item animate-fade-in delay-1">
                        <a class="nav-link nav-btn <%= locals.page === 'dashboard' ? 'active' : '' %>" href="/">
                            <i class="fas fa-home nav-icon"></i>
                            <span class="nav-text">Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item animate-fade-in delay-2">
                        <a class="nav-link nav-btn <%= locals.page === 'team' ? 'active' : '' %>" href="/team">
                            <i class="fas fa-users nav-icon"></i>
                            <span class="nav-text">My Team</span>
                        </a>
                    </li>
                    <li class="nav-item animate-fade-in delay-3">
                        <a class="nav-link nav-btn <%= locals.page === 'investments' ? 'active' : '' %>" href="/investments">
                            <i class="fas fa-chart-line nav-icon"></i>
                            <span class="nav-text">Investments</span>
                        </a>
                    </li>
                    <li class="nav-item animate-fade-in delay-4">
                        <a class="nav-link nav-btn <%= locals.page === 'challenges' ? 'active' : '' %>" href="/challenges">
                            <i class="fas fa-trophy nav-icon"></i>
                            <span class="nav-text">Daily Challenges</span>
                        </a>
                    </li>
                    <li class="nav-item animate-fade-in delay-5">
                        <a class="nav-link nav-btn <%= locals.page === 'offers' ? 'active' : '' %>" href="/offers">
                            <i class="fas fa-gift nav-icon"></i>
                            <span class="nav-text">Offers & VIP</span>
                        </a>
                    </li>
                    <% if (locals.user && locals.user.role === 'admin') { %>
                    <!-- Admin Dashboard link only visible to admin users -->
                    <li class="nav-item animate-fade-in delay-4">
                        <a class="nav-link" href="/admin">
                            <i class="fas fa-user-shield me-2"></i>Admin Panel
                        </a>
                    </li>
                    <% } %>
                </ul>
                <div class="d-flex align-items-center animate-fade-in d-none d-lg-flex">
                    <!-- Notification Dropdown (Desktop only) -->
                    <div class="dropdown me-3 d-none d-lg-block">
                        <a class="nav-link position-relative" href="#" id="notificationDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-bell text-white"></i>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger notification-badge" style="<%= locals.notifications && !locals.notifications.hasUnread ? 'display: none;' : '' %>">
                                <% if (locals.notifications && locals.notifications.items) { %>
                                    <%= locals.notifications.items.filter(n => !n.read).length %>
                                <% } else { %>3<% } %>
                                <span class="visually-hidden">unread notifications</span>
                            </span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end position-absolute notification-dropdown" aria-labelledby="notificationDropdown" style="min-width: 300px; z-index: 9999 !important;">
                            <li><h6 class="dropdown-header py-1"><i class="fas fa-bell me-2"></i>Notifications</h6></li>
                            <li><hr class="dropdown-divider my-1"></li>
                            <% if (locals.notifications && locals.notifications.items && locals.notifications.items.length > 0) { %>
                                <% locals.notifications.items.slice(0, 3).forEach(function(notification) { %>
                                    <li>
                                        <a class="dropdown-item d-flex align-items-center py-1 <%= notification.read ? 'text-muted' : '' %>" href="#">
                                            <div class="rounded-circle icon-bg-<%= notification.bgColor %> p-1 me-2 text-white" style="width: 24px; height: 24px; display: flex; align-items: center; justify-content: center;">
                                                <i class="fas fa-<%= notification.icon %> fa-sm"></i>
                                            </div>
                                            <div style="max-width: calc(100% - 30px); overflow: hidden;">
                                                <p class="mb-0 small <%= notification.read ? '' : 'fw-bold' %>" title="<%= notification.title %>"><%= notification.title %></p>
                                                <small class="text-muted" style="font-size: 0.7rem;" title="<%= notification.message %>"><%= notification.message %></small>
                                            </div>
                                        </a>
                                    </li>
                                    <% if (locals.notifications.items.indexOf(notification) < Math.min(2, locals.notifications.items.length - 1)) { %>
                                        <li><hr class="dropdown-divider my-1"></li>
                                    <% } %>
                                <% }); %>
                            <% } else { %>
                                <li>
                                    <a class="dropdown-item d-flex align-items-center py-1 text-muted" href="#">
                                        <div class="rounded-circle icon-bg-secondary p-1 me-2 text-white" style="width: 24px; height: 24px; display: flex; align-items: center; justify-content: center;">
                                            <i class="fas fa-bell-slash fa-sm"></i>
                                        </div>
                                        <div style="max-width: calc(100% - 30px); overflow: hidden;">
                                            <p class="mb-0 small">No notifications</p>
                                            <small class="text-muted" style="font-size: 0.7rem;">You're all caught up!</small>
                                        </div>
                                    </a>
                                </li>
                            <% } %>
                            <li><hr class="dropdown-divider my-1"></li>
                            <li><a class="dropdown-item text-center py-1 small" href="/notifications"><i class="fas fa-external-link-alt me-1"></i>View all notifications</a></li>
                        </ul>
                    </div>
                    
                    <!-- User Dropdown Menu (Desktop only) -->
                    <div class="dropdown">
                        <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle user-profile-link" id="userDropdown" data-bs-toggle="dropdown" data-bs-auto-close="outside" aria-expanded="false">
                            <div class="rounded-circle me-2 overflow-hidden profile-avatar" style="width: 38px; height: 38px;">
                                <% if (locals.user.avatar) { %>
                                <img src="<%= locals.user.avatar %>" alt="<%= locals.user.name %>" class="w-100 h-100" style="object-fit: cover;">
                                <% } else { %>
                                <div class="bg-primary w-100 h-100 d-flex align-items-center justify-content-center text-white">
                                    <i class="fas fa-user"></i>
                                </div>
                                <% } %>
                            </div>
                            <span class="d-none d-md-inline username-gold"><%= locals.user.name.split(' ')[0] %> <i class="fas fa-caret-down ms-1 opacity-75" style="font-size: 12px;"></i></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end position-absolute premium-profile-dropdown" aria-labelledby="userDropdown" style="width: 260px; z-index: 9999 !important; right: 0; left: auto; margin-top: 5px;">
                            <li class="user-card mb-2">
                                <div class="text-center p-3">
                                    <div class="avatar-large mx-auto mb-2">
                                        <% if (locals.user.avatar) { %>
                                        <img src="<%= locals.user.avatar %>" alt="<%= locals.user.name %>" class="w-100 h-100" style="object-fit: cover;">
                                        <% } else { %>
                                        <div class="bg-primary w-100 h-100 d-flex align-items-center justify-content-center text-white">
                                            <i class="fas fa-user"></i>
                                        </div>
                                        <% } %>
                                    </div>
                                    <h6 class="mb-0 username-gold-large"><%= locals.user.name %></h6>
                                    <small class="text-muted">Member since <%= new Date(locals.user.createdAt).toLocaleDateString() %></small>
                                </div>
                            </li>
                            <li>
                                <a class="dropdown-item menu-item py-2" href="/profile">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-primary me-2">
                                            <i class="fas fa-user-circle"></i>
                                        </div>
                                        <span>My Profile</span>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item menu-item py-2" href="/team">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-info me-2">
                                            <i class="fas fa-users"></i>
                                        </div>
                                        <span>My Team</span>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item menu-item py-2" href="/investments">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-success me-2">
                                            <i class="fas fa-chart-line"></i>
                                        </div>
                                        <span>Investments</span>
                                    </div>
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item menu-item py-2" href="#" id="supportTicketLink">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-info me-2">
                                            <i class="fas fa-headset"></i>
                                        </div>
                                        <span>Support Ticket</span>
                                    </div>
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item menu-item py-2 sign-out-button" href="/sign-out">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle menu-icon icon-danger me-2">
                                            <i class="fas fa-sign-out-alt"></i>
                                        </div>
                                        <span class="fw-bold">Sign Out</span>
                                    </div>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>
    <% } %>

    <% if (!locals.user && (locals.page === 'sign_in' || locals.page === 'sign_up' || locals.page === 'forgot_password' || locals.page === 'reset_password')) { %>
    <!-- Cosmic theme for auth pages (login, signup, forgot password, and reset password) -->
    <section class="vh-100 auth-section">
        <!-- Background stars -->
        <div class="stars-bg" id="stars-bg"></div>
        
        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col-12 col-md-10 col-lg-8 col-xl-7">
                    <div class="card shadow-2-strong cosmic-card" style="border-radius: 1rem;">
                        <div class="card-body p-5 text-center">
                            <%- body %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Add cosmic background script -->
        <script src="/js/cosmic-bg.js"></script>
    </section>
    <% } else { %>
    <!-- Original section for ALL other pages (unchanged) -->
    <section class="min-vh-100" style="<%= !locals.user ? 'background-color: #508bfc;' : '' %>">
        <div class="container py-5">
            <div class="row d-flex justify-content-center">
                <div class="col-12 <%= locals.user ? 'col-lg-10' : 'col-md-8 col-lg-6 col-xl-5' %>">
                    <div class="card shadow-2-strong dashboard-main-card" style="border-radius: 1rem;">
                        <div class="card-body p-5 <%= !locals.user ? 'text-center' : '' %>">
                            <%- body %>
                        </div>
                    </div>
                    <div class="mb-5"></div><!-- Extra space at the bottom for better scrolling -->
                </div>
            </div>
        </div>
    </section>
    <% } %>
    <%- script %>
    <% if (locals.user) { %>
    <script src="/js/cosmic-bg.js"></script>
    <% } %>
    <% if (locals.flash) { %>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            <% if (flash.success && flash.success.length > 0) { %>
            var successMessage = "<%= flash.success %>";
            new Noty({
                theme: 'nest',
                text: successMessage,
                type: 'success',
                layout: 'topRight',
                timeout: 2500
            }).show();
            <% } %>
            
            <% if (flash.error && flash.error.length > 0) { %>
            var errorMessage = "<%= flash.error %>";
            new Noty({
                theme: 'nest',
                text: errorMessage,
                type: 'error',
                layout: 'topRight',
                timeout: 2500
            }).show();
            <% } %>
        });
    </script>
    <% } %>
    
    <!-- IntlTelInput JS for phone number input with country codes -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/intl-tel-input/17.0.19/js/intlTelInput.min.js" referrerpolicy="no-referrer"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/intl-tel-input/17.0.19/js/utils.js" referrerpolicy="no-referrer"></script>
    
    <!-- Scroll position restoration script -->
    <script>
      // Save scroll position before page unload
      if (history.scrollRestoration) {
        history.scrollRestoration = 'manual';
      }
      
      let scrollPos = {};
      
      window.addEventListener('beforeunload', function() {
        scrollPos = {
          x: window.scrollX,
          y: window.scrollY
        };
        sessionStorage.setItem('scrollPos', JSON.stringify(scrollPos));
      });
      
      document.addEventListener('DOMContentLoaded', function() {
        // Restore scroll position after page has fully loaded
        if (sessionStorage.getItem('scrollPos')) {
          const savedPos = JSON.parse(sessionStorage.getItem('scrollPos'));
          // Use a slight delay to ensure all content is rendered
          setTimeout(() => {
            window.scrollTo(savedPos.x, savedPos.y);
          }, 100);
        }
      });
    </script>
    
    <!-- Auto-inject CSRF token into all POST forms -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        document.querySelectorAll('form').forEach(function(form) {
          if (form.method && form.method.toLowerCase() === 'post') {
            var input = document.createElement('input');
            input.type = 'hidden';
            input.name = '_csrf';
            input.value = '<%= csrfToken %>';
            form.prepend(input);
          }
        });
      });
    </script>
  </body>

</html>